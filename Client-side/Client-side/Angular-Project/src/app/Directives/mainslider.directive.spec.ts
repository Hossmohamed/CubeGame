import { MainsliderDirective } from './mainslider.directive';

describe('MainsliderDirective', () => {
  it('should create an instance', () => {
    const directive = new MainsliderDirective();
    expect(directive).toBeTruthy();
  });
});
